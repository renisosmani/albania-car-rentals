# Copy this file to .env.local and adjust values as needed.
#
# SQLite database path (relative to the project root).
# The file is created automatically when you run: npm run db:migrate
DATABASE_URL="file:./dev.db"
